import { ArrowRight, CheckCircle2 } from "lucide-react";
import sessionIntelligence from "../../assets/mira-session-intelligence-approved.png";
import { Reveal } from "../components/ui/Reveal";
import { openEarlyAccess } from "../components/early-access/EarlyAccessModal";
export function ProductPreview(){return <section className="report-wrap" id="product"><div className="section report-section"><Reveal className="report-copy"><p className="eyebrow">Your first report</p><h2>One meeting. Clear risks, evidence, and next actions.</h2><p>Know what still needs attention before it becomes a problem.</p><ul>{["See what your team understood","Find unresolved issues sooner","Trace insights to evidence","Leave with clear ownership"].map(x=><li key={x}><CheckCircle2 size={14}/>{x}</li>)}</ul><a href="#early-access" onClick={(event) => { event.preventDefault(); openEarlyAccess("report_copy", event.currentTarget); }}>Try MIRA free <span>→</span></a></Reveal><Reveal className="report-ui" delay={100}><div className="session-product-frame"><div className="session-product-scroll"><img className="session-product-image" src={sessionIntelligence} alt="MIRA Session Intelligence analysis for the Leadership Offsite" draggable={false}/></div></div></Reveal></div><Reveal className="inline-conversion"><div><strong>See this report for your own session.</strong><span>Start with one recording. Review the evidence. Decide what happens next.</span></div><a className="button button--primary" href="#early-access" onClick={(event) => { event.preventDefault(); openEarlyAccess("report_preview", event.currentTarget); }}>Try MIRA free <ArrowRight size={14}/></a></Reveal></section>}
