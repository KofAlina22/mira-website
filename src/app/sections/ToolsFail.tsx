import { ArrowRight, Check, Eye, FileCheck2, RefreshCw, ShieldCheck, X } from "lucide-react";
import { Reveal } from "../components/ui/Reveal";
import { SectionIntro } from "../components/ui/SectionIntro";
import { openEarlyAccess } from "../components/early-access/EarlyAccessModal";
const rows=[["Analysis",true,true,false],["Human validation",true,false,false],["Evidence-linked insights",true,false,false],["Actionable recommendations",true,"partial",false],["Measurable outcomes",true,false,false]] as const;
const trust=[[ShieldCheck,"Human review","Important conclusions can be checked before action."],[Eye,"Evidence traceability","See the moments behind every material insight."],[FileCheck2,"Beyond summaries","Measure understanding, confidence, and unresolved risk."],[RefreshCw,"Fits current work","Start with one recording from your existing process."]] as const;
export function ToolsFail(){const icon=(v:boolean|string)=>v===true?<Check size={14}/>:v==="partial"?<span>—</span>:<X size={14}/>;return <section className="difference-wrap" id="use-cases"><div className="section difference"><SectionIntro eyebrow="Why teams choose MIRA" title="More than notes. Confidence before action." copy="MIRA combines analysis with validation to produce insights people can inspect and act on."/><div className="difference-layout"><Reveal className="compare-table"><div className="compare-head"><span>Capability</span><b>MIRA</b><span>AI summary</span><span>Manual notes</span></div>{rows.map(row=><div className="compare-row" key={row[0]}><strong>{row[0]}</strong>{row.slice(1).map((v,i)=><span key={i} className={i===0?"mira-cell":""}>{icon(v)}</span>)}</div>)}</Reveal><div className="trust-grid">{trust.map(([Icon,title,text],i)=><Reveal className="trust-card" key={title} delay={i*50}><Icon size={18}/><h3>{title}</h3><p>{text}</p></Reveal>)}</div></div><Reveal className="difference-cta"><div><strong>Ready to see what one session reveals?</strong><span>Upload a recording and inspect the resulting evidence.</span></div><div><a className="button button--primary" href="#early-access" onClick={(event) => { event.preventDefault(); openEarlyAccess("comparison", event.currentTarget); }}>Try MIRA free <ArrowRight size={14}/></a><a href="#product">See the product</a></div></Reveal></div></section>}
